name: P2-10G Hidden Run Key Monolithic Probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Monolithic probe retained for manual diagnostics only; distributed scan is preferred."
